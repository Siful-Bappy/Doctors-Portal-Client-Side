import React from 'react';

const PrimaryButton = () => {
    return (
        <button className="btn uppercase font-bold text-white bg-gradient-to-r from-primary to-secondary">Get Started</button>
    );
};

export default PrimaryButton;